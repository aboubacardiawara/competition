package competition.display;

/**
 * this interface allow to implements class which allow to display messages.
 */
public interface Displayer{
    /**
     * Display message msg
     */
    public void displayMsg(String msg);
}
